============
Installation
============

At the command line::

    $ easy_install django-model-sync

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-model-sync
    $ pip install django-model-sync
